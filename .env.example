# Local development can use the Vite proxy at /api.
# Render production must set VITE_API_BASE_URL=https://smmaibackend.onrender.com/api.
# Do not deploy production with a bare /api value.
VITE_API_BASE_URL=/api
VITE_UPLOAD_STRATEGY=backend_proxy
VITE_LOCAL_DEV_AUTH_BYPASS=false
